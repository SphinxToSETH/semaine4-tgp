<h1>Gossips#edit</h1>

<%= form_tag gossip_path(@gossip), method: :put do %>

<%= render 'form' %> 

	<%= submit_tag "modifies ton gossip"%>
<% end %>


 <td><%= link_to 'Destoy gossip', gossip_path(gossip),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
<%= link_to 'Retour à la liste des gossips', gossip_path %>